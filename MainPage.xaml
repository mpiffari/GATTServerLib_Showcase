<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Showcase.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="50"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            
            <Label
                Text="{Binding GattServerConnectionState}"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level5" />
            
            <Button Command="{Binding OnInitGattServerCommand}" Text="Initialize lib" Margin="2" />
            <Button Command="{Binding OnStartGattServerCommand}" Text="Start GATT server" Margin="2" />
            <Button Command="{Binding OnStopGattServerCommand}" Text="Stop GATT server" Margin="2" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>